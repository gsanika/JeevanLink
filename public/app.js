const socket = window.io ? io() : null;
const page = document.body.dataset.page || 'home';
const NODE = new URLSearchParams(location.search).get('node') || 'B';
const NAMES={A:'Victim',B:'Relay B',C:'Relay C',R:'Rescue Center'};
const NEXT={A:'B',B:'C',C:'R'};
const CHAIN=['A','B','C','R'];
const SEV={critical:0,high:1,medium:2};
let state={online:{A:false,B:false,C:false,R:false},sos:[]};
let toastTimer;
function $(s){return document.querySelector(s)}
function esc(s){const d=document.createElement('div');d.textContent=s??'';return d.innerHTML}
function toast(msg){let t=$('#toast');if(!t){t=document.createElement('div');t.id='toast';t.className='toast';document.body.appendChild(t)}t.textContent=msg;t.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.classList.remove('show'),2800)}
function nav(active){return `<aside class="sidebar"><a class="brand" href="/"><div class="brand-mark">✚</div><div><b>JeevanLink</b><small>resilient emergency network</small></div></a><div class="nav-label">Monitor</div><nav class="nav"><a class="${active==='dashboard'?'active':''}" href="/dashboard.html"><span class="ico">◈</span><span>Command Center</span></a><a class="${active==='incidents'?'active':''}" href="/incidents.html"><span class="ico">🚨</span><span>Incidents</span></a><a class="${active==='network'?'active':''}" href="/network.html"><span class="ico">⌁</span><span>Mesh Network</span></a><a class="${active==='operations'?'active':''}" href="/operations.html"><span class="ico">🚑</span><span>Operations</span></a><div class="nav-label">Resources</div><a class="${active==='responders'?'active':''}" href="/responders.html"><span class="ico">◉</span><span>Responders</span></a><a class="${active==='reports'?'active':''}" href="/reports.html"><span class="ico">▥</span><span>Reports</span></a></nav><div class="side-bottom"><div class="node-pill"><span class="dot on"></span><span>Demo network online</span></div></div></aside>`}
function shell(active,title,crumb='Live operations'){return `<div class="app">${nav(active)}<main class="main"><header class="topbar"><div class="top-title"><div><div class="crumb">JEEVANLINK / ${crumb.toUpperCase()}</div><h1>${title}</h1></div></div><div class="top-actions"><span class="badge ok"><span class="dot on"></span> Demo live</span><button class="icon-btn" onclick="location.href='/victim.html'">🚨</button></div></header><div class="content" id="content"></div></main></div><div id="modal" class="modal-wrap"></div><div id="toast" class="toast"></div>`}
function hops(path){return CHAIN.map((n,i)=>`${i?'<span class="arrow">→</span>':''}<span class="hop ${path?.includes(n)?'reached':''}">${n}</span>`).join('')}
function severityBadge(s){return `<span class="badge ${s}">${s}</span>`}
function timeAgo(ts){const s=Math.max(0,Math.floor((Date.now()-ts)/1000));return s<60?`${s}s ago`:`${Math.floor(s/60)}m ago`}
function mapHTML(items=[]){const defaults=[['critical',20,38,'SOS #82931'],['high',62,26,'Accident'],['relay',44,62,'Relay B'],['relay',71,67,'Relay C'],['rescue',82,35,'Rescue']];const pts=items.length?items.map((s,i)=>[s.severity,i%2?65:25,30+i*12,s.id]):defaults;return `<div class="map"><div class="road r1"></div><div class="road r2"></div><div class="road r3"></div>${pts.map(([type,x,y,label])=>`<div class="map-marker ${type}" style="left:${x}%;top:${y}%">${type==='relay'?'📡':type==='rescue'?'🚑':'!'}</div><div class="map-label" style="left:${x}%;top:${y}%">${esc(label)}</div>`).join('')}<div class="map-legend"><span><i class="legend-dot" style="background:var(--critical)"></i>Critical</span><span><i class="legend-dot" style="background:var(--warn)"></i>High</span><span><i class="legend-dot" style="background:#3478c7"></i>Relay</span><span><i class="legend-dot" style="background:var(--ok)"></i>Rescue</span></div></div>`}
function dashboard(){document.querySelector('.main').outerHTML;$('#content').innerHTML=`<div class="toolbar"><div><div class="eyebrow">Emergency operations</div><h1>Command center</h1><p>Real-time view of incoming SOS signals, network health and response readiness.</p></div><div class="actions"><button class="btn ghost" onclick="demoSOS()">+ Demo SOS</button><button class="btn primary" onclick="openDispatch()">Dispatch center</button></div></div><div class="grid grid-4" id="stats"></div><div class="split" style="margin-top:16px"><section class="panel"><div class="panel-head"><h3>Live incident map</h3><span class="badge ok">● Network synchronized</span></div>${mapHTML(state.sos.filter(s=>s.status==='delivered'))}</section><section class="panel"><div class="panel-head"><h3>Priority queue</h3><span class="badge neutral" id="queueCount">0 active</span></div><div class="incident-list" id="queue"></div></section></div><div class="grid grid-2" style="margin-top:16px"><section class="panel"><div class="panel-head"><h3>Mesh topology</h3><a class="btn ghost" href="/network.html">View network →</a></div><div class="panel-body"><div class="node-flow" id="topology"></div></div></section><section class="panel"><div class="panel-head"><h3>Response timeline</h3><span class="muted" style="font-size:10px">Live event stream</span></div><div class="panel-body" id="timeline"></div></section></div>`;renderDashboard()}
function renderDashboard(){const ss=state.sos;$('#stats').innerHTML=[['red','Critical',ss.filter(s=>s.severity==='critical').length,'Immediate response'],['warn','High',ss.filter(s=>s.severity==='high').length,'Priority response'],['blue','All incidents',ss.length,'Signals received'],['green','Acknowledged',ss.filter(s=>s.ackStatus==='acknowledged').length,'Help dispatched']].map(x=>`<div class="stat ${x[0]}"><div class="stat-top"><span>${x[1]}</span><span>24H</span></div><div class="stat-value">${x[2].toString().padStart(2,'0')}</div><div class="stat-note">${x[3]}</div></div>`).join('');const sorted=[...ss].sort((a,b)=>(SEV[a.severity]??9)-(SEV[b.severity]??9));$('#queue').innerHTML=sorted.length?sorted.slice(0,6).map(s=>`<div class="incident"><div class="incident-top"><div><div class="incident-title">${esc(s.message||'Emergency signal received')}</div><div class="incident-meta">${s.id} · ${s.people||1} people · ${s.injured||0} injured · ${timeAgo(s.createdAt)}</div></div>${severityBadge(s.severity)}</div><div class="path">${hops(s.path)}</div><div style="display:flex;gap:7px;margin-top:11px"><button class="btn ghost" onclick="openIncident('${s.id}')">Inspect</button><button class="btn ${s.ackStatus==='acknowledged'?'success':'primary'}" onclick="ack('${s.id}')">${s.ackStatus==='acknowledged'?'✓ Acknowledged':'Acknowledge'}</button></div></div>`).join(''):`<div class="empty"><div class="big">📡</div>Waiting for incoming SOS signals</div>`;$('#queueCount').textContent=`${ss.length} active`;$('#topology').innerHTML=CHAIN.map((n,i)=>`<div class="relay-node ${state.online[n]?'live':''}">${n}</div>${i<3?'<div class="network-line"></div>':''}`).join('');$('#timeline').innerHTML=sorted.slice(0,5).map(s=>`<div class="timeline-item"><div class="timeline-line"></div><div><b>${esc(s.id)} · ${s.ackStatus==='acknowledged'?'Rescue acknowledged':'SOS received'}</b><small>${new Date(s.createdAt).toLocaleTimeString()} · ${s.severity} priority</small></div></div>`).join('')||'<div class="empty">No events yet</div>'}
function openIncident(id){const s=state.sos.find(x=>x.id===id);if(!s)return;openModal(`<div class="modal-head"><h3>${s.id} · Incident details</h3><button class="close" onclick="closeModal()">×</button></div><div class="modal-body"><div style="display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:18px"><div><div class="eyebrow">Emergency classification</div><h2 style="margin:5px 0 0;font-size:22px">${esc(s.message||'Emergency signal')}</h2></div>${severityBadge(s.severity)}</div><div class="grid grid-2"><div class="stat"><div class="stat-top"><span>People affected</span></div><div class="stat-value">${s.people||1}</div></div><div class="stat"><div class="stat-top"><span>Injured</span></div><div class="stat-value">${s.injured||0}</div></div></div><div class="panel pad" style="margin-top:14px"><div class="eyebrow">Communication route</div><div class="path" style="margin-top:10px">${hops(s.path)}</div><p class="muted" style="font-size:11px;margin:12px 0 0">${s.location?`GPS: ${s.location.lat}, ${s.location.lng}`:'Location unavailable'}</p></div><div class="actions" style="margin-top:16px"><button class="btn ghost" onclick="closeModal()">Close</button><button class="btn primary" onclick="ack('${s.id}');closeModal()">${s.ackStatus==='acknowledged'?'✓ Already acknowledged':'🚑 Acknowledge & dispatch'}</button></div></div>`)}
function openDispatch(){openModal(`<div class="modal-head"><h3>Dispatch rescue team</h3><button class="close" onclick="closeModal()">×</button></div><div class="modal-body"><div class="queue"><div class="queue-row"><div><b>Alpha Rescue · Fire & Search</b><small>2.4 km · 4 min ETA · 5 units</small></div><span class="badge ok">Ready</span></div><div class="queue-row"><div><b>Blue Medic · Medical response</b><small>3.1 km · 7 min ETA · 3 units</small></div><span class="badge high">Deploying</span></div><div class="queue-row"><div><b>Delta Search · Rescue team</b><small>4.2 km · 8 min ETA · 6 units</small></div><span class="badge ok">Ready</span></div></div><div class="actions" style="margin-top:18px"><button class="btn ghost" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="toast('Rescue unit dispatched');closeModal()">Dispatch nearest available</button></div></div>`)}
function ack(id){if(socket)socket.emit('ack:send',{sosId:id});const s=state.sos.find(x=>x.id===id);if(s)s.ackStatus='acknowledged';renderCurrent();toast(`Acknowledgment sent for ${id}`)}
function demoSOS(){const payload={message:'3 people trapped after a building collapse. 1 injured.',people:3,injured:1,severity:'critical',location:{lat:18.5204,lng:73.8567}};if(socket)socket.emit('sos:create',payload);toast('Demo SOS injected into the mesh');}
function incidents(){const filters=['all','critical','high','medium'];$('#content').innerHTML=`<div class="toolbar"><div><div class="eyebrow">Incident management</div><h1>Incident board</h1><p>Inspect, prioritize and acknowledge emergency signals.</p></div><div class="actions">${filters.map(f=>`<button class="btn ${f==='all'?'primary':'ghost'} filter" data-filter="${f}" onclick="filterInc('${f}')">${f}</button>`).join('')}</div></div><section class="panel"><div class="panel-head"><h3 id="incTitle">All incidents</h3><span class="badge neutral">Live</span></div><div class="incident-list" id="incList"></div></section>`;renderIncidents('all')}
function filterInc(f){document.querySelectorAll('.filter').forEach(b=>b.classList.toggle('primary',b.dataset.filter===f));document.querySelectorAll('.filter').forEach(b=>b.classList.toggle('ghost',b.dataset.filter!==f));renderIncidents(f)}
function renderIncidents(f){const list=state.sos.filter(s=>f==='all'||s.severity===f).sort((a,b)=>(SEV[a.severity]??9)-(SEV[b.severity]??9));$('#incTitle').textContent=`${f==='all'?'All':f[0].toUpperCase()+f.slice(1)} incidents (${list.length})`;$('#incList').innerHTML=list.length?list.map(s=>`<div class="incident"><div class="incident-top"><div><div class="incident-title">${esc(s.message||'Emergency signal')}</div><div class="incident-meta">${s.id} · ${timeAgo(s.createdAt)} · ${s.location?`${s.location.lat}, ${s.location.lng}`:'no GPS'}</div></div>${severityBadge(s.severity)}</div><div class="path">${hops(s.path)}</div><div style="display:flex;gap:7px;margin-top:11px"><button class="btn ghost" onclick="openIncident('${s.id}')">View details</button><button class="btn ${s.ackStatus==='acknowledged'?'success':'primary'}" onclick="ack('${s.id}')">${s.ackStatus==='acknowledged'?'✓ Dispatched':'Acknowledge'}</button></div></div>`).join(''):'<div class="empty"><div class="big">✓</div>No incidents in this queue</div>'}
function network(){const q=Object.values(state.online).filter(Boolean).length;$('#content').innerHTML=`<div class="toolbar"><div><div class="eyebrow">Resilient transport layer</div><h1>Mesh network</h1><p>Visualize the demo topology, node health and store-and-forward behavior.</p></div><button class="btn primary" onclick="simulateOutage()">Simulate relay outage</button></div><div class="grid grid-4"><div class="stat green"><div class="stat-top"><span>Nodes online</span></div><div class="stat-value">${q}/4</div><div class="stat-note">A · B · C · R</div></div><div class="stat blue"><div class="stat-top"><span>Packets today</span></div><div class="stat-value">${Math.max(18,state.sos.length*3)}</div><div class="stat-note">Forwarded across hops</div></div><div class="stat warn"><div class="stat-top"><span>Queued packets</span></div><div class="stat-value">${state.online.C?0:2}</div><div class="stat-note">Store-and-forward buffer</div></div><div class="stat red"><div class="stat-top"><span>Median hop</span></div><div class="stat-value">1.4s</div><div class="stat-note">Demo transport delay</div></div></div><div class="grid grid-2" style="margin-top:16px"><section class="panel"><div class="panel-head"><h3>Live topology</h3><span class="badge ok">${q}/4 online</span></div><div class="panel-body"><div class="node-flow">${CHAIN.map((n,i)=>`<div style="text-align:center"><div class="relay-node ${state.online[n]?'live':''}">${n}</div><small style="display:block;color:#718596;font-size:9px;margin-top:7px">${NAMES[n]}</small></div>${i<3?'<div class="network-line"></div>':''}`).join('')}</div><div class="panel pad" style="margin-top:10px"><div class="eyebrow">Store-and-forward</div><p class="muted" style="font-size:11px;line-height:1.7">If the next hop is unavailable, the packet remains queued at the relay and flushes automatically when the route returns.</p></div></div></section><section class="panel"><div class="panel-head"><h3>Node health</h3><span class="muted" style="font-size:10px">Last 60 seconds</span></div><table class="table"><thead><tr><th>Node</th><th>Role</th><th>Status</th><th>Battery</th></tr></thead><tbody>${CHAIN.map((n,i)=>`<tr><td><b>${n}</b></td><td>${NAMES[n]}</td><td><span class="badge ${state.online[n]?'ok':'critical'}">${state.online[n]?'Online':'Offline'}</span></td><td>${[91,82,76,100][i]}%</td></tr>`).join('')}</tbody></table></section></div>`}
function simulateOutage(){if(!socket)return;socket.emit('node:set-online',{nodeId:'C',value:false});toast('Relay C marked out of range — new packets will queue');setTimeout(()=>{socket.emit('node:set-online',{nodeId:'C',value:true});toast('Relay C back in range — queued packets flushing')},6500)}
function operations(){const dispatch=[['Structure collapse near sector 4','critical','4 min','Assigned'],['Traffic accident with trapped driver','high','7 min','En route'],['Medical emergency in residential block','medium','10 min','Queued']];$('#content').innerHTML=`<div class="toolbar"><div><div class="eyebrow">Field response</div><h1>Operations console</h1><p>Coordinate dispatch queues and field teams from one view.</p></div><button class="btn primary" onclick="toast('Operations sync refreshed')">↻ Refresh</button></div><div class="grid grid-2"><section class="panel"><div class="panel-head"><h3>Dispatch queue</h3><span class="badge neutral">3 missions</span></div><div class="incident-list">${dispatch.map(x=>`<div class="incident"><div class="incident-top"><div><div class="incident-title">${x[0]}</div><div class="incident-meta">${x[3]} · ETA ${x[2]}</div></div>${severityBadge(x[1])}</div><div style="margin-top:11px"><button class="btn primary" onclick="toast('Unit reassigned')">Manage mission</button></div></div>`).join('')}</div></section><section class="panel"><div class="panel-head"><h3>Field teams</h3><span class="badge ok">Coverage ready</span></div><div class="panel-body queue">${[['Alpha Rescue','Fire team',88,'Ready'],['Blue Medic','Medical unit',72,'Deploying'],['Delta Search','Search & rescue',91,'Ready']].map(t=>`<div class="queue-row"><div><b>${t[0]}</b><small>${t[1]} · ${t[3]}</small></div><div style="width:130px"><div style="height:6px;background:#172734;border-radius:99px;overflow:hidden"><span style="display:block;width:${t[2]}%;height:100%;background:linear-gradient(90deg,var(--ok),var(--blue))"></span></div><small style="display:block;text-align:right;margin-top:5px">${t[2]}% coverage</small></div></div>`).join('')}</div></section></div>`}
function responders(){const teams=[['🚑','Alpha Rescue','Fire team','Ready','88%','4 min'],['🩺','Blue Medic','Medical unit','Deploying','72%','7 min'],['🚨','Delta Search','Search & rescue','Ready','91%','3 min']];$('#content').innerHTML=`<div class="toolbar"><div><div class="eyebrow">Response resources</div><h1>Responder roster</h1><p>Availability, readiness and estimated response time.</p></div><button class="btn primary" onclick="toast('Roster synchronized')">Sync roster</button></div><div class="grid grid-3">${teams.map(t=>`<div class="panel pad"><div style="display:flex;justify-content:space-between;align-items:center"><div class="relay-node live" style="width:48px;height:48px">${t[0]}</div>${severityBadge(t[3].toLowerCase()==='ready'?'ok':'high')}</div><h3 style="margin:15px 0 4px;font-size:14px">${t[1]}</h3><p class="muted" style="font-size:11px;margin:0 0 18px">${t[2]}</p><div class="stat"><div class="stat-top"><span>Coverage</span><b>${t[4]}</b></div><div style="height:7px;background:#172734;border-radius:99px;margin-top:9px;overflow:hidden"><span style="display:block;width:${t[4]};height:100%;background:linear-gradient(90deg,var(--ok),var(--blue))"></span></div><div style="display:flex;justify-content:space-between;margin-top:13px;font-size:10px;color:#718596"><span>ETA</span><b>${t[5]}</b></div></div></div>`).join('')}</div>`}
function reports(){const bars=[40,68,53,88,62,74,58];$('#content').innerHTML=`<div class="toolbar"><div><div class="eyebrow">Performance intelligence</div><h1>Operational reports</h1><p>Response performance and network reliability indicators.</p></div><button class="btn ghost" onclick="toast('Report export prepared')">Export report</button></div><div class="grid grid-4"><div class="stat green"><div class="stat-top"><span>Avg response</span></div><div class="stat-value">08m</div><div class="stat-note">-12% vs last week</div></div><div class="stat blue"><div class="stat-top"><span>Network coverage</span></div><div class="stat-value">92%</div><div class="stat-note">Relay availability</div></div><div class="stat red"><div class="stat-top"><span>Critical</span></div><div class="stat-value">03</div><div class="stat-note">This reporting period</div></div><div class="stat warn"><div class="stat-top"><span>Resolved</span></div><div class="stat-value">91%</div><div class="stat-note">Within response target</div></div></div><div class="grid grid-2" style="margin-top:16px"><section class="panel"><div class="panel-head"><h3>Incident volume</h3><span class="muted" style="font-size:10px">Last 7 days</span></div><div class="bars">${bars.map((h,i)=>`<div class="bar-col"><span style="height:${h}%"></span></div>`).join('')}</div><div class="panel-body" style="display:flex;justify-content:space-between;color:#718596;font-size:9px"><span>Mon</span><span>Tue</span><span>Wed</span><span>Thu</span><span>Fri</span><span>Sat</span><span>Sun</span></div></section><section class="panel"><div class="panel-head"><h3>Reliability signals</h3></div><div class="panel-body queue">${[['SOS delivery rate','98.7%','ok'],['Acknowledgment rate','94.2%','ok'],['Relay recovery','96.4%','medium'],['Duplicate suppression','99.1%','ok']].map(x=>`<div class="queue-row"><div><b>${x[0]}</b><small>Target ≥ 95%</small></div><span class="badge ${x[2]}">${x[1]}</span></div>`).join('')}</div></section></div>`}
function victim(){document.body.innerHTML=`<div class="sos-layout" style="padding:0 18px 60px"><header class="topbar" style="margin:0 -18px 24px;padding:0 18px;position:sticky"><a class="brand" href="/" style="padding:0"><div class="brand-mark">✚</div><div><b>JeevanLink</b><small>Emergency access</small></div></a><div class="top-actions"><span class="badge ok"><span class="dot on"></span><span id="vNet">Connecting</span></span><a class="btn ghost" href="/">Exit</a></div></header><div class="toolbar"><div><div class="eyebrow">Citizen safety</div><h1>Emergency assistance</h1><p>One tap creates an emergency signal with location and situation details.</p></div></div><section class="sos-card"><div style="display:flex;flex-direction:column;align-items:center;text-align:center;padding:18px 0 30px"><div class="eyebrow">Emergency signal</div><h2 style="font-size:26px;margin:9px 0">Are you in immediate danger?</h2><p class="muted" style="font-size:12px;max-width:480px">Press SOS to start. You can speak your situation, attach GPS and review the emergency before sending.</p><button class="sos-button" id="sosBtn">SOS</button><div style="margin-top:16px;color:#718596;font-size:10px">Tap to create emergency · voice or text available</div></div><div class="status-grid"><div class="mini-status"><small>Network</small><b id="vNetwork">Searching…</b></div><div class="mini-status"><small>Location</small><b id="vLocation">Locating…</b></div><div class="mini-status"><small>Relay</small><b id="vRelay">Ready</b></div><div class="mini-status"><small>Response</small><b id="vResponse">Standby</b></div></div></section><section class="panel" style="margin-top:16px"><div class="panel-head"><h3>Active emergency</h3><span id="vStatusBadge" class="badge neutral">No active SOS</span></div><div class="panel-body" id="vStatus"><div class="empty"><div class="big">🛟</div>Your emergency timeline will appear here.</div></div></section></div><div id="toast" class="toast"></div>`;initVictim()}
let currentLoc=null,currentPhoto=null;
function initVictim(){if(socket)socket.on('connect',()=>socket.emit('register',{nodeId:'A'}));if(socket)socket.on('topology:update',({online})=>{state.online=online;$('#vNet').textContent=online.A?'Mesh connected':'Searching for relays';$('#vNetwork').textContent=online.A?'Connected':'Searching…';$('#vRelay').textContent=online.B?'Relay B available':'Searching';});$('#sosBtn').onclick=()=>openSOSModal();if(navigator.geolocation)navigator.geolocation.getCurrentPosition(p=>{currentLoc={lat:+p.coords.latitude.toFixed(4),lng:+p.coords.longitude.toFixed(4)};$('#vLocation').textContent='GPS attached'},()=>$('#vLocation').textContent='Unavailable');if(socket)socket.on('sos:created',s=>showVictimStatus(s,false));if(socket)socket.on('ack:delivered',({sosId})=>{if($('#vStatusBadge')){$('#vStatusBadge').className='badge ok';$('#vStatusBadge').textContent='Help acknowledged';$('#vResponse').textContent='Rescue notified';renderVictimTimeline(sosId,true);toast('Rescue center acknowledged your SOS')}});if(socket)socket.on('ack:relay',()=>toast('Acknowledgment moving through relay network'))}
function openSOSModal(){openModal(`<div class="modal-head"><h3>Create emergency SOS</h3><button class="close" onclick="closeModal()">×</button></div><div class="modal-body"><div class="field"><label>What happened?</label><textarea id="sosText" placeholder="Example: 3 people are trapped after a building collapse. 1 injured."></textarea></div><div class="grid grid-2"><div class="field"><label>People affected</label><input id="sosPeople" type="number" min="1" value="1"></div><div class="field"><label>Emergency type</label><select id="sosType"><option>General emergency</option><option>Medical</option><option>Fire</option><option>Accident</option><option>Collapse / trapped</option></select></div></div><div class="panel pad" style="margin-bottom:15px"><div style="display:flex;justify-content:space-between;gap:10px;align-items:center"><div><b style="font-size:11px">On-device emergency read</b><p class="muted" style="font-size:10px;margin:4px 0 0">Prototype heuristic classifies urgency locally.</p></div><button class="btn ghost" onclick="voiceFill()">🎙️ Speak</button></div><div id="parsePreview" style="margin-top:10px"></div></div><div class="field"><label>Optional photo</label><input id="sosPhoto" type="file" accept="image/*" capture="environment"></div><div class="panel pad"><div class="eyebrow">Location</div><div style="font:11px var(--mono);margin-top:6px">${currentLoc?`${currentLoc.lat}, ${currentLoc.lng}`:'GPS will be attached if available'}</div></div><div class="actions" style="margin-top:17px"><button class="btn ghost" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="sendSOS()">🚨 Send SOS via mesh</button></div></div>`);$('#sosText').oninput=updateParse;updateParse()}
function parseEmergency(text){const t=(text||'').toLowerCase();const pm=t.match(/(\d+)\s*(people|persons|of us)/);const im=t.match(/(\d+)\s*(injured|hurt|wounded)/);let people=pm?+pm[1]:+(($('#sosPeople')||{}).value||1);let injured=im?+im[1]:(/injur|hurt|wound|bleeding|unconscious/.test(t)?1:0);let severity=/trapped|collapse|unconscious|buried|can't breathe|cant breathe|bleeding heavily|fire|dying/.test(t)?'critical':/injur|hurt|pain|broken|wound|bleeding/.test(t)?'high':'medium';return{people,injured,severity}}
function updateParse(){const p=parseEmergency($('#sosText')?.value||'');if($('#parsePreview'))$('#parsePreview').innerHTML=`${severityBadge(p.severity)} <span class="badge neutral">${p.people} people</span> ${p.injured?`<span class="badge high">${p.injured} injured</span>`:''}`}
function voiceFill(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){toast('Voice input is unavailable here — type instead');return}const r=new SR();r.lang='en-IN';r.continuous=false;r.onstart=()=>toast('Listening… speak your emergency');r.onresult=e=>{$('#sosText').value=e.results[0][0].transcript;updateParse()};r.onerror=()=>toast('Voice capture failed — type your message');r.start()}
function sendSOS(){const text=$('#sosText').value.trim()||'Emergency assistance requested';const p=parseEmergency(text);const payload={message:text,people:p.people,injured:p.injured,severity:p.severity,location:currentLoc};const f=$('#sosPhoto').files[0];if(f){const reader=new FileReader();reader.onload=()=>{payload.photo=reader.result;if(socket)socket.emit('sos:create',payload);closeModal()};reader.readAsDataURL(f)}else{if(socket)socket.emit('sos:create',payload);closeModal()}$('#vStatusBadge').className='badge critical';$('#vStatusBadge').textContent='SOS active';$('#vResponse').textContent='Sending';toast('SOS created — routing through mesh');}
function showVictimStatus(s,ack){$('#vStatusBadge').className='badge critical';$('#vStatusBadge').textContent=ack?'Help acknowledged':'SOS active';renderVictimTimeline(s.id,ack,s)}
function renderVictimTimeline(id,ack,s){const found=s||state.sos.find(x=>x.id===id);const path=found?.path||['A'];$('#vStatus').innerHTML=`<div class="grid grid-2"><div class="panel pad"><div class="eyebrow">Incident</div><div style="font:700 22px var(--mono);margin:7px 0">${id}</div>${found?`${severityBadge(found.severity)} <span class="badge neutral">${found.people} people</span>`:''}<div class="path" style="margin-top:14px">${hops(path)}</div></div><div class="panel pad"><div class="eyebrow">Delivery status</div><div class="timeline" style="margin-top:8px"><div class="timeline-item"><div class="timeline-line"></div><div><b>Emergency created</b><small>Signal generated on this device</small></div></div><div class="timeline-item"><div class="timeline-line"></div><div><b>Relay route discovered</b><small>Packet moving through available nodes</small></div></div><div class="timeline-item"><div class="timeline-line"></div><div><b>${ack?'Rescue center acknowledged':'Waiting for rescue acknowledgment'}</b><small>${ack?'Help request received by command center':'You can keep this screen open for updates'}</small></div></div></div></div></div>`}
function relay(){const next=NEXT[NODE];document.body.innerHTML=`<div class="sos-layout" style="padding:0 18px 60px"><header class="topbar" style="margin:0 -18px 24px;padding:0 18px"><a class="brand" href="/" style="padding:0"><div class="brand-mark">✚</div><div><b>JeevanLink</b><small>Relay node console</small></div></a><div class="top-actions"><span class="badge ok"><span class="dot on"></span> Node ${NODE}</span><a class="btn ghost" href="/dashboard.html">Command Center</a></div></header><div class="toolbar"><div><div class="eyebrow">Mesh relay</div><h1>Relay ${NODE}</h1><p>This screen demonstrates receiving, storing, forwarding and acknowledging packets.</p></div><button class="btn primary" id="rangeBtn">● IN RANGE</button></div><div class="grid grid-4"><div class="stat green"><div class="stat-top"><span>Node status</span></div><div class="stat-value" id="rStatus">ONLINE</div><div class="stat-note">Simulated radio presence</div></div><div class="stat blue"><div class="stat-top"><span>Next hop</span></div><div class="stat-value">${next}</div><div class="stat-note">${NAMES[next]}</div></div><div class="stat warn"><div class="stat-top"><span>Packets</span></div><div class="stat-value" id="rPackets">00</div><div class="stat-note">Received this session</div></div><div class="stat red"><div class="stat-top"><span>Hop delay</span></div><div class="stat-value">1.4s</div><div class="stat-note">Demo transport</div></div></div><div class="grid grid-2" style="margin-top:16px"><section class="panel"><div class="panel-head"><h3>Packet feed</h3><span class="badge neutral" id="rFeedCount">0 packets</span></div><div class="incident-list" id="rFeed"><div class="empty">Waiting for packets…</div></div></section><section class="panel"><div class="panel-head"><h3>Route state</h3><span class="badge ok" id="rRoute">Forwarding ready</span></div><div class="panel-body"><div class="node-flow">${CHAIN.map((n,i)=>`<div class="relay-node ${n===NODE||state.online[n]?'live':''}">${n}</div>${i<3?'<div class="network-line"></div>':''}`).join('')}</div><div class="panel pad" style="margin-top:12px"><div class="eyebrow">Store-and-forward rule</div><p class="muted" style="font-size:11px;line-height:1.7">If ${next} is unavailable, incoming SOS packets remain queued until the next hop returns.</p></div></div></section></div></div><div id="toast" class="toast"></div>`;let packets=0,onlineSelf=true;socket?.on('connect',()=>socket.emit('register',{nodeId:NODE}));socket?.on('topology:update',({online})=>{state.online=online;const on=online[NODE];onlineSelf=on;$('#rStatus').textContent=on?'ONLINE':'OFFLINE';$('#rangeBtn').textContent=on?'● IN RANGE':'○ OUT OF RANGE';$('#rangeBtn').className='btn '+(on?'primary':'warn')});$('#rangeBtn').onclick=()=>{onlineSelf=!onlineSelf;socket?.emit('node:set-online',{nodeId:NODE,value:onlineSelf});};socket?.on('sos:incoming',s=>{packets++;$('#rPackets').textContent=String(packets).padStart(2,'0');$('#rFeedCount').textContent=`${packets} packets`;const el=document.createElement('div');el.className='incident';el.innerHTML=`<div class="incident-top"><div><div class="incident-title">${s.id}</div><div class="incident-meta">${esc(s.message||'Emergency signal')} · ${timeAgo(s.createdAt)}</div></div>${severityBadge(s.severity)}</div><div class="path">${hops(s.path)}</div><div style="margin-top:10px;font-size:10px;color:#8ea1b3">📥 Received → stored → forwarding to ${next}</div>`;$('#rFeed').querySelector('.empty')?.remove();$('#rFeed').prepend(el);toast(`Packet ${s.id} received at Relay ${NODE}`)});socket?.on('ack:relay',({sosId})=>toast(`ACK ${sosId} relayed toward victim`))}
function home(){document.body.innerHTML=`<div class="landing"><header class="landing-nav"><a class="brand"><div class="brand-mark">✚</div><div><b>JeevanLink</b><small>resilient emergency network</small></div></a><div class="top-actions"><span class="badge ok"><span class="dot on"></span> Prototype live</span><a class="btn ghost" href="/dashboard.html">Command Center</a></div></header><section class="hero-center"><div class="eyebrow">Emergency communication infrastructure</div><h1 class="hero-title">When networks fail,<br><span>people become the link.</span></h1><p class="lead">JeevanLink is a hackathon prototype for resilient emergency communication. An SOS can move through nearby relay nodes using a store-and-forward workflow before reaching a rescue command center.</p><div class="actions" style="justify-content:center;margin-top:25px"><a class="btn primary" href="/victim.html">🚨 Open emergency app</a><a class="btn ghost" href="/dashboard.html">Explore command center</a></div><div class="chain"><div class="chain-node"><div class="emoji">👤</div><b>Victim</b><small>CREATE SOS</small></div><div class="chain-arrow">→</div><div class="chain-node"><div class="emoji">📡</div><b>Relay B</b><small>STORE / FORWARD</small></div><div class="chain-arrow">→</div><div class="chain-node"><div class="emoji">📡</div><b>Relay C</b><small>STORE / FORWARD</small></div><div class="chain-arrow">→</div><div class="chain-node"><div class="emoji">🚑</div><b>Rescue</b><small>ACKNOWLEDGE</small></div></div></section><div class="portal-grid"><a class="portal" href="/victim.html"><div class="picon">🚨</div><h3>Victim app</h3><p>Create SOS with voice, GPS and emergency details.</p></a><a class="portal" href="/relay.html?node=B"><div class="picon">📡</div><h3>Relay B</h3><p>Watch packets arrive, queue and forward.</p></a><a class="portal" href="/relay.html?node=C"><div class="picon">📡</div><h3>Relay C</h3><p>Simulate an out-of-range relay and recovery.</p></a><a class="portal" href="/dashboard.html"><div class="picon">🛰️</div><h3>Rescue command</h3><p>Prioritize incidents, inspect routes and acknowledge SOS.</p></a></div></div>`}
function openModal(inner){let m=$('#modal');if(!m){m=document.createElement('div');m.id='modal';m.className='modal-wrap';document.body.appendChild(m)}m.innerHTML=`<div class="modal">${inner}</div>`;m.classList.add('show')}
function closeModal(){const m=$('#modal');if(m)m.classList.remove('show')}
function renderCurrent(){if(page==='dashboard')renderDashboard();if(page==='incidents')renderIncidents('all')}
function boot(){if(page==='home'){home();return}if(page==='victim'){victim();return}if(page==='relay'){relay();return}const titles={dashboard:['dashboard','Command center'],incidents:['incidents','Incident board'],network:['network','Mesh network'],operations:['operations','Operations console'],responders:['responders','Responder roster'],reports:['reports','Operational reports']};const [active,title]=titles[page]||titles.dashboard;document.body.innerHTML=shell(active,title);if(page==='dashboard')dashboard();if(page==='incidents')incidents();if(page==='network')network();if(page==='operations')operations();if(page==='responders')responders();if(page==='reports')reports();if(socket){socket.on('connect',()=>socket.emit('register',{nodeId:page==='dashboard'||page==='incidents'||page==='network'||page==='operations'||page==='responders'||page==='reports'?'R':'R'}));socket.on('topology:update',d=>{state.online=d.online;});socket.on('sos:bootstrap',list=>{state.sos=list||[];renderCurrent()});socket.on('sos:new',s=>{const i=state.sos.findIndex(x=>x.id===s.id);if(i>=0)state.sos[i]=s;else state.sos.push(s);renderCurrent();toast(`New ${s.severity} SOS received`)});}}
boot();
